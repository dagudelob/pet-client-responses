{
  "mcpServers": {
    "gmail-rover-listener": {
      "command": "python",
      "args": ["-m", "src.mail_listener"],
      "env": {
        "ROVER_EMAIL_FILTER": "from:rover.com",
        "FETCH_INTERVAL_MINUTES": "5"
      },
      "description": "MCP Server to ingest Rover notifications and messages through the Gmail API"
    }
  }
}
